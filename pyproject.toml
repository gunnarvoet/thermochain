[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"

[project]
name = "thermodrift"
version = "2026.03.0"
authors = [
    {name = "Gunnar Voet", email = 'gvoet@ucsd.edu'},
]
description = "Moored thermistor processing"
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["physical oceanography"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Programming Language :: Python :: 3",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Oceanography",
    ]
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "numpy>=1.26.2",
    "xarray>=2023.12.0",
    "matplotlib>=3.8.2",
    "gsw>=3.6.17",
    "pyyaml>=6.0.1",
    "python-box>=7.1.1",
    "openpyxl>=3.1.2",
    "tqdm",
    "mixsea",
    "sbemoored",
    "rbrmoored",
    "gvpy",
    "odfpy>=1.4.1",
]

[project.urls]
# Homepage = "https://example.com"
# Documentation = "https://readthedocs.org"
Repository = "https://github.com/gunnarvoet/thermodrift.git"
Issues = "https://github.com/gunnarvoet/thermodrift/issues"
Changelog = "https://github.com/gunnarvoet/thermodrift/blob/main/HISTORY.md"

[tool.pytest.ini_options]
# in general, do not place a `__init__.py` in your tests
# options are explained here:
# https://learn.scientific-python.org/development/guides/pytest/
# minversion = "6.0"
# addopts = ["-ra", "--showlocals", "--strict-markers", "--strict-config", "--import-mode=importlib"]
# xfail_strict = true
# filterwarnings = ["error"]
# log_cli_level = "info"
testpaths = ["tests"]
filterwarnings = [
    "error",
    # note the use of single quote below to denote "raw" strings in TOML
    'ignore:invalid escape sequence:DeprecationWarning',
    # netCDF4 wheels ship with an older numpy ABI stamp — cosmetic,
    # the module works at runtime. Triggered via gvpy.trilaterate
    # importing netCDF4 at module load.
    'ignore:numpy\.ndarray size changed:RuntimeWarning',
]

[tool.pdm]
package-type = "library"

[tool.uv.sources]
sbemoored = { path = "../sbemoored", editable = true }
rbrmoored = { path = "../rbrmoored", editable = true }
gvpy = { path = "../gvpy", editable = true }
# mixsea = { path = "../mixsea", editable = true }
mixsea = { git = "https://github.com/modscripps/mixsea", branch = "main" }


[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.1",
    "pdoc>=15.0.0",
]
